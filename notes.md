we have to print documents and sign them, someone not on the computer for compliance , so we need to print. and keep scan 
we either full digital , digitial print , maybe something for finger print but that is for later [icebox]. 
the other options is we print it and then we scan it once fillled. 

so two input optons . 

**fisical**
query gets you the file empty
next step requries scan

**digital**
first step you input the fiels you can input . we get file with the space to sign. here we can still print it or sign it with a device. digital signature later. 

so two options 

  **print and sign and scan**
  we get a way to download the file or print it then next step asks us to upload scan. 
  
  **all digital baby**
  here again two optons dgital signarue or like an iphone. 
  
    **digital signature** 
    we send the file to someone , or we just make it donwlaodable another prgram deals with sendi it . we now need to upload a file 

    **manual diigitized signature** 
    we send a link to a tmp site where a canvas allows to sign and read doc singature is saved. the link can be a qr code. 

lets find comonalities, the follwing step is gonna need . either a scanfile, a digitally signed file , or a signature file. 
if scan file is gonna be missnig some info so we need to type stuff. or ocr, 

so two routes/endpoints 

1. that shows(allows to touch sign) creates tmp website or download the contract/document
  if it shows we need to create a qr code or link and also need the input date or not.
  the page is gonna have to have a route to send the stuff this is route 2
  if donwload same . 

2. this populates the document table. 
we either got all the info from show . so this there already sna dpopulates table documents as signed. 

we had to download and print in wich case we need manually fill the data. or ocr we . perhaps we need a field for it to know based on wich solicitud the personal this person came for , we might not now as we might have more than one solicitud de personal at a time. we might also want to know how did they hear about us.
if donloaded we iether printed or we didnt and we got a signed parper document or we got a digital document . in both cases we end up with a digital document we need to scan the paper one. 

we also need to sttore the file somewhere . in wich format tho . we do need to imput data to two files can we do that with pdf? i think yes pdf and yes we can edit . so first route . we either fill info or not we either mention about wich solicitud the personal is or not. we either get a downldoable pdf or we get the qr code for the page that contiant the siging route and the siging canvas. or we get the form and the siging canvas lets start with downldoable. :q: 
